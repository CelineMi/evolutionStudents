{% extends 'base.html.twig' %}

{% block title %}Week{% endblock %}

{% block body %}
    <h1>Week</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ week.id }}</td>
            </tr>
            <tr>
                <th>WeekNumber</th>
                <td>{{ week.weekNumber }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ week.userId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('week_index') }}">back to list</a>

    <a href="{{ path('week_edit', {'id': week.id}) }}">edit</a>

    {{ include('week/_delete_form.html.twig') }}
{% endblock %}
